repeat task.wait() until game:IsLoaded()

--// CLEAN UI
pcall(function()
    game.CoreGui:FindFirstChild("BANANA_KEY"):Destroy()
end)

--// KEY
local correctKey = "BANANA555"

--// MAIN FUNCTION
function startBANANA()

-- LOAD UI
local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

-- SERVICES
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- CONFIG
local Config = {
    ESP = true,
    Aimbot = false,
    SilentAim = false,
    TeamCheck = true,
    WallCheck = false,
    Hitbox = false,
    HitboxSize = 5,
    FOV = 200
}

-- UI
local Window = Rayfield:CreateWindow({
    Name = "BANANA ELITE 🍌💀",
    LoadingTitle = "BANANA ELITE",
    LoadingSubtitle = "Warfare System",
    ConfigurationSaving = {Enabled = false}
})

local Combat = Window:CreateTab("Combat")
local Visual = Window:CreateTab("Visual")

-- FOV
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 2
FOVCircle.NumSides = 100
FOVCircle.Filled = false
FOVCircle.Color = Color3.fromRGB(255,0,0)

-- FUNCTIONS
local function GetHealth(c)
    local h=c:FindFirstChildOfClass("Humanoid")
    return h and h.Health or 0
end

local function GetTeam(p)
    return p.Team and p.Team.Name or "Unknown"
end

local function GetColor(team)
    team = tostring(team):lower()
    if team:find("red") then
        return Color3.fromRGB(255,50,50)
    elseif team:find("blue") then
        return Color3.fromRGB(50,150,255)
    end
    return Color3.fromRGB(255,255,255)
end

local function IsEnemy(p)
    if not Config.TeamCheck then return true end
    return GetTeam(p) ~= GetTeam(LocalPlayer)
end

local function IsVisible(part)
    if not Config.WallCheck then return true end
    local origin = Camera.CFrame.Position
    local direction = (part.Position - origin)

    local params = RaycastParams.new()
    params.FilterDescendantsInstances = {LocalPlayer.Character}
    params.FilterType = Enum.RaycastFilterType.Blacklist

    local result = workspace:Raycast(origin, direction, params)
    return not result or result.Instance:IsDescendantOf(part.Parent)
end

-- ESP
local ESP = {}

local function CreateESP(p)
    if p == LocalPlayer then return end
    if ESP[p] then return end

    local bill = Instance.new("BillboardGui")
    bill.Size = UDim2.new(0,200,0,50)
    bill.AlwaysOnTop = true
    bill.Parent = game.CoreGui

    local txt = Instance.new("TextLabel")
    txt.Size = UDim2.new(1,0,1,0)
    txt.BackgroundTransparency = 1
    txt.TextScaled = true
    txt.TextStrokeTransparency = 0
    txt.Font = Enum.Font.SourceSansBold
    txt.Parent = bill

    ESP[p] = {GUI=bill,Text=txt}
end

for _,p in pairs(Players:GetPlayers()) do CreateESP(p) end
Players.PlayerAdded:Connect(CreateESP)

-- REMOVE ESP (FIX LAG)
Players.PlayerRemoving:Connect(function(p)
    if ESP[p] then
        if ESP[p].GUI then ESP[p].GUI:Destroy() end
        ESP[p] = nil
    end
end)

-- TARGET
local LockedTarget=nil

local function GetTarget()
    local closest=nil
    local shortest=Config.FOV^2

    for _,p in pairs(Players:GetPlayers()) do
        if p~=LocalPlayer and p.Character then
            local c=p.Character
            local head=c:FindFirstChild("Head")

            if head and GetHealth(c)>0 and IsEnemy(p) and IsVisible(head) then
                local pos,vis=Camera:WorldToViewportPoint(head.Position)

                if vis then
                    local dx=pos.X-Camera.ViewportSize.X/2
                    local dy=pos.Y-Camera.ViewportSize.Y/2
                    local dist=dx*dx+dy*dy

                    if dist<shortest then
                        shortest=dist
                        closest=head
                    end
                end
            end
        end
    end

    LockedTarget=closest
end

-- UI CONTROLS
Combat:CreateToggle({Name="Aimbot (Head Lock)",Callback=function(v)Config.Aimbot=v end})
Combat:CreateToggle({Name="Silent Aim (Soft)",Callback=function(v)Config.SilentAim=v end})
Combat:CreateToggle({Name="Team Check",Callback=function(v)Config.TeamCheck=v end})
Combat:CreateToggle({Name="Wall Check",Callback=function(v)Config.WallCheck=v end})
Combat:CreateToggle({Name="Hitbox Expander",Callback=function(v)Config.Hitbox=v end})

Combat:CreateSlider({
    Name="Hitbox Size",
    Range={2,20},
    CurrentValue=5,
    Callback=function(v)Config.HitboxSize=v end
})

Visual:CreateToggle({Name="ESP",Callback=function(v)Config.ESP=v end})

Visual:CreateSlider({
    Name="FOV Size",
    Range={50,400},
    CurrentValue=200,
    Callback=function(v)Config.FOV=v end
})

-- LOOP (OPTIMIZED)
task.spawn(function()
    while task.wait(0.05) do

        FOVCircle.Radius = Config.FOV
        FOVCircle.Position = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
        FOVCircle.Visible = Config.Aimbot

        GetTarget()

        if Config.Aimbot and LockedTarget then
            Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, LockedTarget.Position)
        end

        if Config.SilentAim and LockedTarget then
            Camera.CFrame = Camera.CFrame:Lerp(
                CFrame.lookAt(Camera.CFrame.Position, LockedTarget.Position),
                0.25
            )
        end

        for _,p in pairs(Players:GetPlayers()) do
            if p~=LocalPlayer then
                local esp=ESP[p]
                local c=p.Character

                if not esp or not c then continue end

                local root=c:FindFirstChild("HumanoidRootPart")

                if not root or GetHealth(c)<=0 or not Config.ESP or not IsEnemy(p) then
                    esp.GUI.Enabled=false
                    continue
                end

                local hp=math.floor(GetHealth(c))
                local dist=math.floor((root.Position-Camera.CFrame.Position).Magnitude)
                local team=GetTeam(p)
                local color=GetColor(team)

                esp.GUI.Adornee=root
                esp.GUI.Enabled=true
                esp.Text.Text="[BANANA ELITE] "..p.Name.." ["..team.."]\nHP: "..hp.." | "..dist.."m"
                esp.Text.TextColor3=color

                if Config.Hitbox then
                    root.Size=Vector3.new(Config.HitboxSize,Config.HitboxSize,Config.HitboxSize)
                    root.Transparency=0.5
                    root.CanCollide=false
                else
                    root.Size=Vector3.new(2,2,1)
                    root.Transparency=0
                    root.CanCollide=true
                end
            end
        end

    end
end)

end

--// KEY UI (ALWAYS SHOW)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BANANA_KEY"
ScreenGui.Parent = game.CoreGui

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0,300,0,150)
Frame.Position = UDim2.new(0.5,-150,0.5,-75)
Frame.BackgroundColor3 = Color3.fromRGB(25,25,25)
Frame.Parent = ScreenGui

Instance.new("UICorner", Frame)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1,0,0,40)
Title.BackgroundTransparency = 1
Title.Text = "BANANA ELITE KEY 🍌"
Title.TextColor3 = Color3.fromRGB(255,255,255)
Title.TextScaled = true
Title.Parent = Frame

local Box = Instance.new("TextBox")
Box.Size = UDim2.new(0.8,0,0,40)
Box.Position = UDim2.new(0.1,0,0.4,0)
Box.PlaceholderText = "Enter Key..."
Box.Text = ""
Box.TextScaled = true
Box.BackgroundColor3 = Color3.fromRGB(40,40,40)
Box.TextColor3 = Color3.fromRGB(255,255,255)
Box.Parent = Frame

local Btn = Instance.new("TextButton")
Btn.Size = UDim2.new(0.6,0,0,35)
Btn.Position = UDim2.new(0.2,0,0.75,0)
Btn.Text = "ENTER"
Btn.TextScaled = true
Btn.BackgroundColor3 = Color3.fromRGB(255,200,0)
Btn.TextColor3 = Color3.fromRGB(0,0,0)
Btn.Parent = Frame

Btn.MouseButton1Click:Connect(function()
    if Box.Text == correctKey then
        ScreenGui:Destroy()
        startBANANA()
    else
        Btn.Text = "WRONG KEY ❌"
        task.wait(1)
        Btn.Text = "ENTER"
    end
end)
